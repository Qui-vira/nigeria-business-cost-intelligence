# Power BI Desktop writes CRLF. Normalise so a Desktop save is not a whole-file diff.
*.tmdl   text eol=crlf
*.pbir   text eol=crlf
*.pbism  text eol=crlf
*.pbip   text eol=crlf
*.json   text eol=crlf
*.csv    text eol=lf

